// 실버 2 - 22114번 : 창영이와 점프
// 작성자 : free4760(jeonghoe22)

#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

int main()
{
    // 입출력 최적화
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    // 보도 블럭 개수 N, 창영이의 보폭 K
    int N, K;
    cin >> N >> K;

    vector<int> dists(N-1, 0);
    
}