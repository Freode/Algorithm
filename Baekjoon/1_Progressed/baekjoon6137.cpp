// 골드 4 - 6137번 : 문자열 생성
// 작성자 : free4760(jeonghoe22)

#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

int main()
{
    // 입출력 최적화
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);

    // 문자열 길이 N
    int N;
    cin >> N;

    // 문자열 입력
    string str = "";
    string input;
    for(int i = 0; i < N; i++)
    {
        cin >> input;
        str += input;
    }

    int start = 0;
    int end = input.length()-1;

    string 

}