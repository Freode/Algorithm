// 골드 5 - 1593번 : 문자 해독
// 작성자 : free4760(jeonghoe22)

#include <iostream>
#include <vector>
#include <algorithm>
#include <string>

using namespace std;

int simulate(const string& W, const string& S)
{
    int result = 0;
    int start = 0, end = 0;
    

    return result;
}

int main()
{
    // 입출력 최적화
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    // 그림문자 g, 문자열 s
    int g, s;
    cin >> g >> s;

    string W, S;
    cin >> W >> S;



}